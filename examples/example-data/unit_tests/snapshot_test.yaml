suite: example/data snapshot tests
templates:
  - deploy.yaml
tests:
  - it: manifest snapshot
    asserts:
      - matchSnapshot: {}
  - it: summary snapshot
    set: 
      summary: true
    asserts:
      - matchSnapshot: {}
